from ws import BackboneCollection

class SensorController(BackboneCollection)

    def __init__():
        self.ws = None # make the websocket connection + send auth
        BackboneCollection.__init__(self)